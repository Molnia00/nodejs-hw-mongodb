openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts app
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: This is a documentation of students app
tags:
  - name: Contacts
    description: Operations about users.

servers:
  - url: https://nodejs-hw-mongodb-4yp1.onrender.com/.

paths:
  /contacts/{id}:
    get:
      $ref: '../swagger/paths/contacts/{id}/get.yaml'
    delete:
      $ref: '../swagger/paths/contacts/delete.yaml'
    patch:
      $ref: '../swagger/paths/contacts/patch.yaml'

  /contacts:
    get:
      $ref: '../swagger/paths/contacts/get.yaml'

    post:
      $ref: '../swagger/paths/contacts/post.yaml'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
